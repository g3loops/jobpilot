# Credentials — never commit or submit
.env
code/.env

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Large artifacts (regenerated by build_index.py)
data/artifacts/*.npy

# OS
.DS_Store

# Large data (raw dump + derived snapshot/artifacts — regenerated by build_index.py, too big for GitHub)
data/techmap-jobs-dump-2021-09.json
data/jobs_snapshot.csv
data/artifacts/
data/*.json
data/*.csv
